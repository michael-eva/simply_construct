/** @type {import('next').NextConfig} */
const nextConfig = {}

// next.config.js

module.exports = {
    images: {
        domains: ['img.mbiz.web.id'], // Add the domain of your image source here
    },
};

